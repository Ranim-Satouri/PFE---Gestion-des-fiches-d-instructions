import { HasPermissionDirectiveDirective } from './has-permission.directive';

describe('HasPermissionDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HasPermissionDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
